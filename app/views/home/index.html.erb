<h1 >Web Rails CV (Home#index)</h1>
<p>Find me in app/views/home/index.html.erb</p>

<%#= This is a comment %>
<%#= The important thing here is that we are using ruby loop syntax in html to extract info from a database %>

<% @articles.each do |article| %>
	
	<div class="mb-3 text-gray-500 dark:text-gray-400">

	<%#= To link a header into a specific route for the post %>
	<h2> <%= link_to article.title, article_path(article) %> </h2>

	<%=  article.title %> 

	</div>
	
<% end %>